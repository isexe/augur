Dump all the .sh files in your ~ directory, make sure they're all executable, and
then run the following command "source installer.sh". The username and password are automatically 
set to 'augur' and 'password'.